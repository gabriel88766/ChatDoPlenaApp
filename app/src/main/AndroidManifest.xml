<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="domicroaomacrogv.blogspot.chatdoplenaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChangeEMail"></activity>
        <activity android:name=".ChangePassword" />
        <activity android:name=".ChangeUser" />
        <activity android:name=".CreateRoom" />
        <activity android:name=".RemoveUserFromRoom" />
        <activity android:name=".AddUserToRoom" />
        <activity android:name=".RemoveRoom" />
        <activity android:name=".CreateUser" />
        <activity android:name=".ListRoom" />
        <activity android:name=".AdminSignedIn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChatActivity" />
        <activity android:name=".UserManagement" />
        <activity android:name=".RoomManagement" />
        <activity android:name=".RemoveUser" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>